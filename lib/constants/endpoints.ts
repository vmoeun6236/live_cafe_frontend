export const API_ENDPOINTS = {
  PRODUCTS: '/products',
  ORDERS: '/orders',
  CATEGORIES: '/categories',
  TABLES: '/tables',
  AUTH: {
    LOGIN: '/auth/login',
    REGISTER: '/auth/register',
  }
} as const;
